'use server'

export async function submitRating(sessionId: string, score: number) {
  // Rules enforced here:
  // - Only mentee can rate
  // - Session must be validated
  // - One rating per session max
  // TODO: implement
}
